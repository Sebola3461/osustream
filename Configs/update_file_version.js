const { getConfigs } = require(".")

module.exports = function() {
    /**
     * disableCommands=Enabled
chatErrorMessage=Enabled
chatJoinConfirmation=Enabled
cooldown=0
     */
    let configs = getConfigs();
    configs.bot
}